{
  "name": "task-management",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "build": "npm install && npm run build --workspace=purplepulse-frontend",
    "start": "npm start --workspace=purplepulse-backend"
  }
}
